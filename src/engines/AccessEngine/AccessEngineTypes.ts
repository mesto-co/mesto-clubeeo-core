export namespace AccessEngineTypes {
  export interface IMember {
    id: string,
    clubId: string,
    userId: string,
  }

  export interface IClub {
    id: string,
  }

  export interface IUser {
    id: string
  }
}
