export interface IContextData {
  traceId?: number
  userId?: number
  clubId?: number
  chain?: number
}

export default class ContextData implements IContextData {
  traceId?: number
  userId?: number
  clubId?: number
  chain?: number
}
